%{
  # Checksums are populated by `mix rustler_precompiled.download ExH3o.Native --all`
  # after the CI release workflow uploads NIF artifacts to the GitHub release.
  #
  # This file MUST be committed to the repository and included in the hex package.
  # Run the download task after each release to regenerate checksums.
}
